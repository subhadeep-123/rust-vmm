[package]
name = "linux-loader"
version = "0.14.0"
authors = [
    "The rust-vmm maintainers",
    "rust-vmm AWS maintainers <rust-vmm-maintainers@amazon.com>",
    "Cathy Zhang <cathy.zhang@intel.com>"
]
edition = "2021"
license = "Apache-2.0 AND BSD-3-Clause"
description = "A Linux kernel image loading crate."
keywords = ["kernel"]
repository.workspace = true
readme = "README.md"
autobenches = false

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[features]
default = ["pe"]
bzimage = ["elf"]
pe = ["elf"]
elf = []

[dependencies]
# We disable the default features for vm-memory since otherwise it would enable "rawfd",
# which isn't supported on Windows (leading to a build-time failure) and we don't use it.
vm-memory = { path = "../vm-memory", version = "0.18", default-features = false }

[dev-dependencies]
criterion = { version = "0.7.0", features = ["html_reports"] }
vm-memory = { path = "../vm-memory", version = "0.18", features = ["backend-mmap"] }

[[bench]]
name = "main"
harness = false

[lib]
bench = false # https://bheisler.github.io/criterion.rs/book/faq.html#cargo-bench-gives-unrecognized-option-errors-for-valid-command-line-options
