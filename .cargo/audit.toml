[advisories]
ignore = [
    # bincode is unmaintained. Now that versionize is gone, the only thing
    # pulling it into the workspace is the vm-superio-ser dev-dependency
    # used by its tests, so it is not part of anything we publish.
    "RUSTSEC-2025-0141",
]
